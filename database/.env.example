# PostgreSQL container credentials
# Copy these values into the root .env file used by docker compose.
POSTGRES_DB=nextkode
POSTGRES_USER=nextkode
POSTGRES_PASSWORD=change_me

# Backend database connection string
# In Docker Compose, the backend connects to PostgreSQL by service name: database.
DATABASE_URL=postgresql://nextkode:change_me@database:5432/nextkode
