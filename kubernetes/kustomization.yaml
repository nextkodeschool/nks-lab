apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  # For CI/CD, workflows create this Secret from GitHub Actions Secrets.
  - secret.yaml
  - database.yaml
  - backend.yaml
  - frontend.yaml
# Enable this only when your cluster has an Ingress controller.
# - ingress.yaml
