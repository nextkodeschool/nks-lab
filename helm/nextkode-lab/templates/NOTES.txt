Next Kode School Lab has been installed.

Namespace:
  {{ .Release.Namespace }}

Frontend service:
  kubectl get svc {{ .Values.frontend.service.name }} -n {{ .Release.Namespace }}

Port forward for local access:
  kubectl port-forward svc/{{ .Values.frontend.service.name }} 8080:{{ .Values.frontend.service.port }} -n {{ .Release.Namespace }}

Open:
  http://localhost:8080

Backend health check:
  kubectl port-forward svc/{{ .Values.backend.service.name }} 8000:{{ .Values.backend.service.port }} -n {{ .Release.Namespace }}
  curl http://localhost:8000/health
